<h1>Editing perfil</h1>

<% form_for(@perfil) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :descricao_perfil %><br />
    <%= f.text_field :descricao_perfil %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @perfil %> |
<%= link_to 'Back', perfils_path %>